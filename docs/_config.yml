# Build settings
markdown: kramdown

url: https://rocky.icpc.io # the base hostname & protocol for your site
baseurl: "" # the subpath of your site, e.g., /blog/

exclude: [
 package.json,
 Gruntfile.js,
 node_modules,
 .sass-cache,
 Gemfile,
 Gemfile.lock,
 .gitignore
]

# Theme Settings
analytics: 
google_verification:

# Contact form
api_token: # Enter API token here


